This project is based on the Depth Anything 

Install 
1. pip install -r requirements.txt to install required library
2. pip uninstall torch torchvision torchaudio -y to install cuda PyTorch
3. pip install torch torchvision torchaudio --index-url 
4. pip install python-osc
5. pip install xformers
